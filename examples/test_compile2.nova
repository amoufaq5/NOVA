let lex_tokens = 0

fn lx_init() {
    lex_tokens = list_new()
}

fn lx_emit(typ, val) {
    let tok = list_new()
    push(tok, typ)
    push(tok, val)
    push(lex_tokens, tok)
}

lx_init()
lx_emit(4, "hello")
lx_emit(0, 0)
println(len(lex_tokens))
